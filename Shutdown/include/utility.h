#ifndef UTILITY_H // If UTILITY_H not defined,
#define UTILITY_H // define it to prevent further inclusion

void clearInputBuffer(); // Clears the input buffer to prevent unwanted characters from being processed.

#endif // End of conditional inclusion